/**
 * The API is served by this same site's Pages Functions at /api/*, so by
 * default no URL is needed — leave API_BASE empty (same origin).
 * Only set a full URL here if you host the API somewhere else.
 */
window.MYTRAIN_CONFIG = {
  API_BASE: "",
};
