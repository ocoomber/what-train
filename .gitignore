# Cloudflare / Wrangler
.dev.vars
.wrangler/

# Node
node_modules/
npm-debug.log*

# OS / editor
.DS_Store
*.swp
.vscode/
.idea/

# Claude Code — commit shared project config (CLAUDE.md, principles, commands)
# so remote/phone sessions load them; ignore only machine-local settings.
.claude/settings.local.json
